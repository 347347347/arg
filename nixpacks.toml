[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "poppler_utils", "libpng", "libjpeg", "zlib"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
